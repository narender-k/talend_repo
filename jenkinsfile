pipeline{
 agent any
 stages{
    stage('Non Prod Talend : Build'){
        steps{
            script{
                sh 'echo running non prod talend scripts'
                sh 'talend --version'
                if(env.BRANCH_NAME == 'dev'){
                    {
                      sh 'terraform --version
                    }
                }
            }
        }
    }
    }
}
